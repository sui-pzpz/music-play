# XX音乐播放器PRD评审纪要

**日期**：2026-06-08
**评审文档**：前端产品需求设计文档V1.0、后端需求设计文档V1.0
**评审方式**：线上同步评审
**参会角色**：产品经理、UI设计师、前端开发、后端开发、测试工程师

---

## 一、评审结论
**评审结果**：有条件通过，需会后确认需求
**总体评价**：两份PRD覆盖范围广、结构清晰，8大模块的功能划分和交互流程基本完整。但存在功能模式、前后端不一致、流程图缺失等问题，需会后补充确认需求。

---

## 二、问题清单（共22项）

### 🔴 重大问题（必改，影响功能正确性）

| # | 角色 | 问题 | 位置 | 修改方案 |
|---|------|------|------|----------|
| 1 | 产品 | **播放模式数量不一致**：前端PRD写3种（列表循环/单曲循环/随机），原始需求要4种（顺序/循环/单曲/随机），现有代码也实现了4种 | 前端PRD 2.6 播放模式 | 统一为4种：顺序播放→列表循环→单曲循环→随机播放，补充顺序播放的交互说明 |
| 2 | 产品 | **待播放队列交互缺失**：术语定义提到了"待播放队列"，但8大模块中没有待播放队列的交互流程 | 前端PRD 模块2 | 新增 2.9 待播放队列子模块：添加到待播、查看待播队列、清空/上下移动、播放优先说明 |
| 3 | 后端 | **音乐-歌手多对多关系缺失**：t_song只有artist_id（主唱），合唱歌曲无法存储多个歌手 | 后端PRD 4.2 | 新增 t_song_artist 关联表(song_id, artist_id, role[main/feat], sort_order)，t_song的artist_id改为默认主唱字段 |
| 4 | 后端 | **第三方登录回调接口缺失**：前端有微信/QQ登录，后端无OAuth回调接口 | 后端PRD 3.1 | 新增 POST /api/v1/auth/oauth/callback 接口（provider, code, state），以及第三方账号绑定表 t_oauth_account |
| 5 | 产品 | **收藏与喜欢定义不清晰**："我喜欢的音乐"和"收藏歌曲"是否同一概念未明确 | 前端PRD 模块4/5 | 明确定义：喜欢歌曲=收藏歌曲（同一操作，同一数据），收藏歌单=收藏他人创建的歌单（不同操作） |

### 🟡 中等问题（建议改，影响体验或一致性）

| # | 角色 | 问题 | 位置 | 修改方案 |
|---|------|------|------|----------|
| 6 | UI | **深色主题默认值冲突**：2.1说"深色主题（默认）"，8.3说"主题默认跟随系统" | 前端PRD 2.1/8.3 | 统一为"默认跟随系统"，2.1删除"（默认）"改为"支持深色/浅色双主题" |
| 7 | 前端 | **播放列表面板交互缺失**：底部播放栏有"列表"按钮，但点击后的面板交互未描述 | 前端PRD 模块2 | 新增 2.10 播放列表面板：点击列表按钮→右侧滑出、显示当前播放列表/待播队列、支持拖拽排序 |
| 8 | 前端 | **歌词全屏路径缺失**：歌词全屏功能在交互中提到但路由菜单无对应路径 | 前端PRD 菜单 | 路由菜单新增 /lyrics 全屏歌词页面 |
| 9 | 后端 | **付费回调接口鉴权方式不明确**：付费回调由第三方平台调用，不能用Token鉴权 | 后端PRD 3.6 | 付费回调接口明确为"无需Token鉴权，通过签名验证"，补充签名验证机制说明 |
| 10 | 后端 | **播放历史表定义缺失**：前端说保留100首，后端无清理定时任务说明 | 后端PRD 第十一章 | 定时任务新增"清理播放历史"：每日清理超过100首的历史记录（按uid分组保留最新100首） |
| 11 | 后端 | **收藏上限校验缺失**：前端说收藏上限10000首，后端接口未体现 | 后端PRD 3.3 | POST /api/v1/user/favorites/:songId 接口补充：校验用户收藏数上限（10000首） |
| 12 | 后端 | **t_playlist_song外键类型不一致**：关联表用BIGINT自增主键，但业务层用VARCHAR的playlist_id/song_id | 后端PRD 4.6 | 统一使用自增id作为外键关联（BIGINT），业务层通过JOIN查询获取业务ID |
| 13 | 产品 | **下载功能描述不完整**：前端有下载管理，后端无下载相关接口，Web端和PC端下载方式不同 | 前端PRD 5.4 / 后端PRD | 前端补充说明：Web端下载使用播放地址+Service Worker缓存，PC端使用专用下载接口。后端新增 GET /api/v1/song/:id/download（PC端专用，返回文件流） |
| 14 | 产品 | **通知功能交互缺失**：顶部导航栏有通知图标，但无通知相关交互流程 | 前端PRD | 新增 3.5 通知中心：点击通知图标→通知列表、已读/未读、通知类型（系统/会员/互动） |

### 🟢 轻微问题（可改，优化完善）

| # | 角色 | 问题 | 位置 | 修改方案 |
|---|------|------|------|----------|
| 15 | UI | **移动端布局结构未单独描述**：仅描述了Web端顶部导航 | 前端PRD 2.2 | 补充移动端布局说明：底部Tab导航+全屏内容区+迷你播放器 |
| 16 | 前端 | **搜索快捷键冲突**：Ctrl+K在Chrome中有默认行为 | 前端PRD 3.1 | 补充说明：优先使用Ctrl+K，冲突时降级为点击搜索框 |
| 17 | 后端 | **搜索接口缺少高亮返回**：搜索结果通常需要高亮匹配词 | 后端PRD 3.4 | 搜索接口响应新增 highlight 字段，返回高亮片段 |
| 18 | 后端 | **用户行为日志表数据量问题**：高写入导致表膨胀 | 后端PRD 4.14 | 补充说明：按月分表，历史数据定期迁移至HDFS |
| 19 | 后端 | **t_song缺少created_at索引**：新歌推荐需按时间排序 | 后端PRD 4.2 | created_at 字段新增 IDX 索引 |
| 20 | 后端 | **通知服务接口缺失**：有notify-service但无接口定义 | 后端PRD 3 | 新增 3.9 通知服务接口：获取通知列表、标记已读、未读数量 |
| 21 | 测试 | **验收标准缺少边界值测试**：如歌单播放全部、首歌循环时间上下限 | 前端PRD 附录 | 验收标准补充边界值测试项 |
| 22 | 测试 | **并发场景验收不足**：如同时收藏同一歌单、多端同时修改歌单 | 后端PRD 第十三章 | 验收标准补充并发场景测试项 |

---

## 三、待确认事项

| # | 事项 | 需要确认的内容 | 负责人 |
|---|------|----------------|--------|
| 1 | 歌词格式 | 是否支持逐字滚动，还是仅逐行显示 | UI设计师 |
| 2 | 音质切换 | 切换音质是否需要重新加载音频 | 前端开发 |
| 3 | 缓存策略 | 播放历史是否需要本地缓存 | 前端开发 |
| 4 | 会员权益 | 无损音质是否仅限VIP用户 | 产品经理 |

---

## 四、后续计划

| 时间节点 | 任务 | 负责人 |
|----------|------|--------|
| 2026-06-10 | 完成重大问题修改 | 产品经理 |
| 2026-06-12 | 完成中等问题修改 | 前后端开发 |
| 2026-06-13 | 待确认事项反馈 | 相关负责人 |
| 2026-06-15 | PRD终版确认 | 全体评审人员 |

---

**记录人**：XXX  
**抄送**：技术负责人、产品负责人