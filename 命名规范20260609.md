# 命名规范文档

## 文档信息

- 文档名称：命名规范
- 生成日期：2026-06-09

---

## 一、文档命名规范

### 规则

**文档意义 + 生成日期**

格式：`{文档意义}{YYYYMMDD}.md`

### 示例

| 文档意义 | 生成日期 | 文件名 |
|---------|---------|--------|
| 命名规范 | 2026-06-09 | 命名规范20260609.md |
| 接口设计 | 2026-06-09 | 接口设计20260609.md |
| 需求分析 | 2026-07-01 | 需求分析20260701.md |
| 测试报告 | 2026-08-15 | 测试报告20260815.md |

---

## 二、代码命名规范

### 2.1 通用原则

- 命名应具有描述性，能清晰表达意图
- 避免使用缩写，除非是广泛认可的（如 ID、URL、HTTP）
- 避免使用单字母命名（循环变量除外）
- 命名不应包含类型信息（匈牙利命名法）

### 2.2 变量命名

- 使用 **camelCase**（小驼峰）

```
userName
orderList
totalPrice
isActive
```

### 2.3 常量命名

- 使用 **UPPER_SNAKE_CASE**（全大写下划线）

```
MAX_RETRY_COUNT
DEFAULT_TIMEOUT
API_BASE_URL
```

### 2.4 函数/方法命名

- 使用 **camelCase**（小驼峰）
- 以动词开头，表达动作意图

```
getUserInfo()
calculateTotal()
isValid()
onButtonClick()
```

### 2.5 类/接口命名

- 使用 **PascalCase**（大驼峰）

```
UserService
OrderController
HttpResponse
IRepository（接口以 I 开头）
```

### 2.6 文件/模块命名

- 使用 **kebab-case**（短横线连接）

```
user-service.ts
order-controller.js
api-utils.py
```

### 2.7 数据库命名

- 表名：**snake_case**（下划线连接），使用复数形式
- 字段名：**snake_case**（下划线连接）

```
表名：user_accounts, order_items
字段名：user_name, created_at, is_active
```

---

## 三、Git 分支与提交命名规范

### 3.1 分支命名

格式：`{类型}/{简短描述}`

| 类型 | 说明 | 示例 |
|------|------|------|
| feature | 新功能 | feature/user-login |
| fix | 修复缺陷 | fix/payment-error |
| refactor | 重构 | refactor/order-module |
| docs | 文档 | docs/api-guide |
| release | 发布 | release/v1.2.0 |

### 3.2 提交信息

格式：`{类型}: {简要描述}`

```
feat: 添加用户登录功能
fix: 修复支付金额计算错误
refactor: 重构订单模块
docs: 更新接口文档
```

---

## 四、项目命名规范

- 项目名：**kebab-case**，全小写
- 包名/命名空间：**kebab-case** 或 **snake_case**

```
my-project
order-service
user-management
```
